/**
 * Vitar's official public-facing support email — shown to clinics and
 * patients across marketing pages, the clinic dashboard, and billing UI.
 * One constant, imported everywhere, so changing it again later is a
 * one-line edit instead of a multi-file hunt (exactly the problem this
 * fixes: it used to be hardcoded separately in 9 different files).
 */
export const VITAR_SUPPORT_EMAIL = 'contact@livevault.cloud';
